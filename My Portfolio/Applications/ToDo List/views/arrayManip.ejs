exports.addItem = function(req, array){
    var item = req.body.newItem;
    array.push(item);
}

exports.removeItem = function(req, array){
    var item = req.body.removeTask; 
    const delIndex = array.indexOf("item");
    array.splice(delIndex, 1);
}